{
  "name": "miedzy-nami",
  "private": true,
  "type": "module",
  "version": "0.9.3",
  "description": "PWA dla par z grą Kto bardziej? na żywo na dwóch telefonach oraz wspólnym silnikiem multiplayer.",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "npm run check && vite build",
    "build:prod": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "typecheck": "tsc --noEmit",
    "content:export": "node scripts/export-library.mjs",
    "content:report": "node scripts/card-report.mjs",
    "test": "node --test tests/*.test.mjs release-v08.test.mjs foundation-v09.test.mjs multiplayer-v091.test.mjs multiplayer-v092.test.mjs multiplayer-v093.test.mjs",
    "check:files": "node validate-v08.mjs",
    "check:release": "node check-release.mjs",
    "check": "npm run content:export && npm run content:report && npm run check:release && npm run check:files && npm run typecheck && npm test",
    "deploy": "npm run build && npx vercel --prod"
  },
  "dependencies": {
    "@supabase/supabase-js": "2.57.4"
  },
  "devDependencies": {
    "typescript": "5.9.2",
    "vite": "7.1.5"
  },
  "engines": {
    "node": "22.x",
    "npm": "10.x"
  },
  "packageManager": "npm@10.9.2"
}
