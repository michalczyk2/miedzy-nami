{
  "name": "miedzy-nami",
  "private": true,
  "version": "0.8.3",
  "description": "Stabilna PWA dla par: dwa telefony, bezpieczne aktualizacje, przyjazne błędy i opcjonalne gry Pikantne 18+.",
  "scripts": {
    "content:export": "node scripts/export-library.mjs",
    "content:report": "node scripts/card-report.mjs",
    "test": "node --test tests/*.test.mjs release-v08.test.mjs",
    "check:files": "node validate-v08.mjs",
    "check": "npm run content:export && npm run content:report && npm run check:release && npm run check:files && npm test",
    "preview": "node scripts/preview.mjs",
    "deploy": "npm run check && npx vercel --prod",
    "check:release": "node check-release.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
