{
  "name": "miedzy-nami",
  "private": true,
  "version": "0.8.2",
  "description": "Intuicyjna PWA dla par z synchronizacją dwóch telefonów i czterema opcjonalnymi grami Pikantne 18+.",
  "scripts": {
    "content:export": "node scripts/export-library.mjs",
    "content:report": "node scripts/card-report.mjs",
    "test": "node --test tests/*.test.mjs release-v08.test.mjs",
    "check:files": "node validate-v08.mjs",
    "check": "npm run content:export && npm run content:report && npm run check:files && npm test",
    "preview": "node scripts/preview.mjs",
    "deploy": "npm run check && npx vercel --prod"
  },
  "engines": {
    "node": ">=20"
  }
}
