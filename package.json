{
  "name": "miedzy-nami",
  "private": true,
  "version": "0.7.1",
  "description": "PWA dla par z czytelnym kontem, wspólną historią i synchronizacją dwóch telefonów.",
  "scripts": {
    "content:export": "node scripts/export-library.mjs",
    "content:report": "node scripts/card-report.mjs",
    "test": "node --test tests/*.test.mjs tests/runtime-smoke.mjs",
    "check:files": "node scripts/validate.mjs",
    "check": "npm run content:export && npm run content:report && npm run check:files && npm test",
    "preview": "node scripts/preview.mjs",
    "deploy": "npm run check && npx vercel --prod"
  },
  "engines": {
    "node": ">=20"
  }
}
